#include <bits/stdc++.h>
#define ll long long int

using namespace std;

void solve() {
    string s;
    cin >> s;
    ll k;
    cin >> k;
    vector<string> l(k);
    for (ll i = 0; i < k; i++) {
        cin >> l[k];
    }

    
}

int main() {
    ll t;
    cin >> t;
    for (ll i = 1; i <= t; i++) {
        cout << "Case #" << t << ": ";
        solve();
    }
    return 0;
}